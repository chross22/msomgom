% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_occupancy_map}
\alias{plot_occupancy_map}
\title{Map posterior occupancy probability across the study grid}
\usage{
plot_occupancy_map(
  fit,
  arrays,
  season = NULL,
  stat = c("mean", "sd"),
  main = NULL,
  ...,
  year = NULL
)
}
\arguments{
\item{fit}{an \code{mcmc.list} that tracked \code{Z} (e.g. from
\code{fit_occupancy_model()}), or a path to a saved \code{.RData} file containing
one}

\item{arrays}{the list returned by \code{build_detection_arrays()} (must be the
same one \code{fit} was fit against, so sites/grid cells line up)}

\item{season}{which absolute season to show (the same index
\code{plot_detection_history()}, \code{season_windows_from_config()}, and the
detection arrays use); defaults to the last season tracked in \code{fit}}

\item{stat}{\code{"mean"} (the default) or \code{"sd"}: the posterior mean of \code{Z} is
the occupancy map, and its posterior SD is the uncertainty map - a cell
the chains never agree on shows up bright in \code{"sd"} and unremarkable in
\code{"mean"}}

\item{main}{plot title; auto-generated from \code{season}/\code{stat} if \code{NULL}}

\item{...}{passed on to \code{plot()}}

\item{year}{deprecated older name for \code{season}, kept so existing calls
keep working - it always meant the arrays' 3rd-dimension index, which is
the absolute season}
}
\value{
invisibly, \code{arrays$area_grid_sf} with an \code{occupancy} column added
(posterior mean or SD of \code{Z} for the chosen season, per \code{stat})
}
\description{
Plots posterior mean occupancy (the latent state \code{Z}) per grid cell for
one year, from a fit run with \code{Z} tracked (\code{jags_params = "Z"} in the
config, or \code{pars = "Z"}). \code{evaluate_occupancy_model()}'s
\code{compare_naive_vs_modeled_occupancy()} reports the same modeled-occupancy
numbers as a per-year table; this puts them on the map instead.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config) # config$jags$params must be "Z"
plot_occupancy_map(fit, arrays)
plot_occupancy_map(fit, arrays, season = 2)
}
}
\seealso{
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}, \code{\link[=plot_sightings]{plot_sightings()}}, \code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_process_map]{plot_process_map()}} for the fitted psi/phi/gamma surfaces of a
\code{"colext"} fit (which tracks no \code{Z} to map),
\code{\link[=compare_naive_vs_modeled_occupancy]{compare_naive_vs_modeled_occupancy()}} for the same numbers as a table,
\code{\link[=load_mcmc_list]{load_mcmc_list()}} for how a saved \code{.RData} path is resolved

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
