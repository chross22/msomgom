% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jags.R
\name{build_whale_model_code}
\alias{build_whale_model_code}
\title{Generate the BUGS/JAGS model code}
\usage{
build_whale_model_code(n_cov_psi, n_cov_phi, n_cov_gamma)
}
\arguments{
\item{n_cov_psi}{number of covariates on initial occupancy (\code{psi})}

\item{n_cov_phi}{number of covariates on persistence (\code{phi})}

\item{n_cov_gamma}{number of covariates on colonization (\code{gamma})}
}
\value{
character string of BUGS/JAGS model code, ready to \code{writeLines()} to a \code{.bug} file
}
\description{
Builds the BUGS/JAGS model code for the dynamic occupancy model, with
covariate blocks on psi/phi/gamma included only for processes that have
\verb{n_cov_* > 0} (see \code{fit_occupancy_model()}'s documentation for why). With
\code{n_cov_psi = n_cov_phi = n_cov_gamma = 0}, this generates exactly the
original intercept-only model.
}
\examples{
cat(build_whale_model_code(0, 0, 0))       # intercept-only, the original model
cat(build_whale_model_code(1, 0, 2))       # 1 covariate on psi, 2 on gamma, none on phi
}
\seealso{
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, which calls this internally

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}}
}
\concept{pipeline stages}
