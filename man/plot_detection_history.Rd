% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_detection_history}
\alias{plot_detection_history}
\title{Plot the detection history the model actually sees}
\usage{
plot_detection_history(arrays, species, main = NULL, ...)
}
\arguments{
\item{arrays}{the list returned by \code{build_detection_arrays()}}

\item{species}{species code; must be a name in \code{arrays$species_arrays}}

\item{main}{plot title; auto-generated from \code{species} if \code{NULL}}

\item{...}{passed on to \code{image()}}
}
\value{
invisibly, the \verb{<site x season>} state matrix: \code{NA} = unsurveyed,
\code{0} = surveyed without a detection, \code{1} = detected
}
\description{
One tile per site x season: was the cell surveyed, and if so, was the
species detected? This is the entire information content of the occupancy
model's likelihood in one figure - everything else (covariates, priors)
only shapes how these tiles are interpolated.
}
\details{
Read it for structure before fitting: a season that is almost all grey
(unsurveyed) will pull its occupancy estimates toward the prior and the
colonization/persistence dynamics, not the data; a species whose detections
are confined to a few sites across every season is a candidate for a
spatially-varying detection covariate rather than more iterations.
}
\examples{
\dontrun{
arrays <- build_detection_arrays(prep$tmpdat, prep$season_info, config)
plot_detection_history(arrays, "RIWH")
}
}
\seealso{
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}, \code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}}, \code{\link[=build_detection_arrays]{build_detection_arrays()}}, which produces
\code{arrays}

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
