% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jagsPrep.R
\name{build_detection_arrays}
\alias{build_detection_arrays}
\title{Build the spatial grid and detection-history/covariate arrays}
\usage{
build_detection_arrays(tmpdat, season_info, config)
}
\arguments{
\item{tmpdat}{the \code{tmpdat} data.frame from \code{prep_survey_data()}}

\item{season_info}{the \code{season_info} list from \code{prep_survey_data()}}

\item{config}{a config list, as returned by \code{load_config()}}
}
\value{
list with:
\item{species_arrays}{named list of one \verb{<site x visit x season>} array per configured species code}
\item{effort3d}{\verb{<site x visit x season>} array of survey trackline length per grid cell}
\item{jday3d}{\verb{<site x visit x season>} array of day-of-year}
\item{bft3d}{\verb{<site x visit x season>} array of mean Beaufort sea state}
\item{reps}{\verb{<season x site>} matrix of repeat-visit counts}
\item{max_survs}{maximum number of surveys in any one season}
\item{num_cells}{number of grid cells (sites)}
\item{num_ssn}{number of year x season combinations}
\item{area_grid_sf}{the hex grid, as an \code{sf} polygon object with a \code{grid_id} column}
}
\description{
Builds a spatial grid over the configured study area, then fills 3D arrays
(site x visit x season) of species detection histories and detection
covariates (survey effort, day-of-year, Beaufort sea state) for JAGS.
}
\details{
Based on the most complete of what used to be three near-duplicate scripts
(\code{legacy/master.R}, \code{legacy/real_deal_linestring6.R}, and the pre-refactor
\code{jagsPrep.R}): this one already included dropping surveys whose track
never enters the grid, and the \code{make_figs} flag.
}
\examples{
\dontrun{
config <- load_config("configs/bof_riwh.yaml")
prep <- prep_survey_data(config)
arrays <- build_detection_arrays(prep$tmpdat, prep$season_info, config)
arrays$num_cells
}
}
\seealso{
\code{\link[=prep_survey_data]{prep_survey_data()}}, which produces \code{tmpdat}/\code{season_info};
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, the next pipeline stage, which takes this
function's return value directly as \code{arrays}

Other pipeline stages:
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}}
}
\concept{pipeline stages}
