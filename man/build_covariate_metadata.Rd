% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jags.R
\name{build_covariate_metadata}
\alias{build_covariate_metadata}
\title{Build the covariate metadata table attached to a fitted model}
\usage{
build_covariate_metadata(psi_cov, phi_cov, gamma_cov)
}
\arguments{
\item{psi_cov, phi_cov, gamma_cov}{the \code{list(n_cov, array, meta)} results of
\code{fit_occupancy_model()}'s internal \code{process_covariate_array()}}
}
\value{
a data.frame with columns \code{name}, \code{process}, \code{prefix}, \code{index},
\code{n_cov}, \code{mean}, \code{sd}, \code{min}, \code{max} - one row per configured covariate -
or \code{NULL} if no process has any covariates configured
}
\description{
One row per (process, covariate): which named coefficient tracks it in the
\code{mcmc.list}, and the raw-scale mean/sd/min/max needed to convert between the
standardized scale the model was fit on and the covariate's own units.
Attached to \code{fit_occupancy_model()}'s return value as the
\code{"dynocc_covariates"} attribute.
}
\seealso{
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, which calls this;
\code{effect_estimates.dynocc_fit()} (in \code{R/effect_estimates.R}), which reads it
}
\keyword{internal}
