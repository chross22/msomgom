% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{parse_survey_time}
\alias{parse_survey_time}
\title{Read a survey TIME column, whatever clock format it's written in}
\usage{
parse_survey_time(x)
}
\arguments{
\item{x}{the raw \code{TIME} column}
}
\value{
numeric \code{hhmmss}
}
\description{
\code{TIME} is \code{hhmmss} in 24-hour form (NARWC handbook 8.A.37), and that's what
this pipeline stores. Real files also write \code{"12:34:56"}, \code{"12:34"}, and
whole timestamps like \code{"2024-04-01T12:34:56Z"} - and \code{as.numeric()} turns
every one of those into \code{NA} without a word, so a file with a perfectly good
clock arrives with no times at all. Preferring \code{TrkTime_UTC} (see
\code{\link[=standardize_survey_columns]{standardize_survey_columns()}}) makes that more likely, since a GPS track
log is exactly where a clock-formatted time comes from, but it was always
possible for \code{Time_UTC}.
}
\details{
The first clock-looking piece of the string is taken, so a bare time and a
full timestamp both work (an ISO date separates with \code{-}, so it can't be
mistaken for one). Seconds are optional and default to zero. A column that's
already numeric is returned untouched.
}
\keyword{internal}
