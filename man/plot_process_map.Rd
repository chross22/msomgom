% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_process.R
\name{plot_process_map}
\alias{plot_process_map}
\title{Map a fitted process probability across the study grid}
\usage{
plot_process_map(
  fit,
  arrays,
  process = c("psi", "phi", "gamma"),
  occ_covariates = NULL,
  season = NULL,
  stat = c("mean", "sd"),
  main = NULL,
  ...
)
}
\arguments{
\item{fit}{a \code{dynocc_fit} from \code{fit_occupancy_model()} (a \code{"colext"} fit)}

\item{arrays}{the list returned by \code{build_detection_arrays()} (the same one
\code{fit} was fit against)}

\item{process}{\code{"psi"}, \code{"phi"}, or \code{"gamma"} - see \code{\link[=predict.dynocc_fit]{predict.dynocc_fit()}}}

\item{occ_covariates}{the same named list of covariate matrices
\code{fit_occupancy_model()} was given; required when \code{process} has covariates}

\item{season}{which season's covariate values to map (\code{1:arrays$num_ssn});
defaults to the last season}

\item{stat}{\code{"mean"} (the default) or \code{"sd"} for the posterior-uncertainty
map}

\item{main}{plot title; auto-generated if \code{NULL}}

\item{...}{passed on to \code{plot()}}
}
\value{
invisibly, \code{arrays$area_grid_sf} with the full
\code{\link[=predict.dynocc_fit]{predict.dynocc_fit()}} summary columns (\code{mean}, \code{sd}, \code{lower}, \code{upper})
added
}
\description{
Draws \code{\link[=predict.dynocc_fit]{predict.dynocc_fit()}}'s surface: posterior mean (or posterior SD,
the uncertainty map) of initial occupancy, persistence, or colonization,
per grid cell. This is the spatial payoff of a covariate fit - where the
model thinks the species persists, colonizes, or starts out present - and
the \code{"sd"} map is the honest companion: a striking mean surface over cells
the posterior barely constrains is a prior in a costume.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config, occ_covariates = list(sst = sst_avg$sst))
plot_process_map(fit, arrays, "psi", occ_covariates = list(sst = sst_avg$sst))
plot_process_map(fit, arrays, "gamma", occ_covariates = list(sst = sst_avg$sst),
                 stat = "sd")
}
}
\seealso{
\code{\link[=predict.dynocc_fit]{predict.dynocc_fit()}} for the numbers; \code{\link[=plot_occupancy_map]{plot_occupancy_map()}}
for realized occupancy from a \code{"Z"} fit; \code{\link[=plot_covariate_map]{plot_covariate_map()}} for the
covariate that shaped the surface

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
