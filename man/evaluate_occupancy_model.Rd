% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_model.R
\name{evaluate_occupancy_model}
\alias{evaluate_occupancy_model}
\title{Evaluate a fitted occupancy model}
\usage{
evaluate_occupancy_model(
  fit,
  arrays = NULL,
  config = NULL,
  output_dir = NULL,
  save_plots = TRUE
)
}
\arguments{
\item{fit}{an \code{mcmc.list} (e.g. from \code{fit_occupancy_model()}), or a path to
a saved \code{.RData} file containing one}

\item{arrays}{optional; the list returned by \code{build_detection_arrays()}.
Required for the naive-vs-modeled occupancy comparison when \code{fit} tracked \code{Z}.}

\item{config}{optional; a config list, as returned by \code{load_config()}.
Used to resolve \code{output_dir} and for the naive-vs-modeled occupancy comparison.}

\item{output_dir}{directory to save \code{mcmc_diagnostics.pdf} to; defaults to
\code{config$paths$output_dir}, or the working directory if \code{config} is also \code{NULL}}

\item{save_plots}{logical; whether to save trace/density plots}
}
\value{
invisibly, \verb{list(parameters = <summary data.frame>, occupancy_comparison = <data.frame or NULL>)}
}
\description{
Reports MCMC convergence diagnostics (Gelman-Rubin Rhat, effective sample
size), a posterior parameter summary table, trace/density plots, and (when
the model was run with \code{jags.params = "Z"}) a comparison of naive vs.
model-estimated occupancy per year.
}
\details{
Does not change or re-derive anything about the model itself (see
\code{jags.R}) - a real posterior-predictive goodness-of-fit check would need
derived nodes added to \code{whale.mod}, which is a model-design change, not an
evaluation one.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config)
evaluate_occupancy_model(fit, arrays, config)

# or, to re-evaluate a previously saved run without re-fitting:
evaluate_occupancy_model("output/mock_test/RIWH.colext.RData", config = config)
}

}
\seealso{
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, which produces \code{fit}; \code{\link[=load_mcmc_list]{load_mcmc_list()}}
for how a saved \code{.RData} path is resolved; \code{\link[=compare_naive_vs_modeled_occupancy]{compare_naive_vs_modeled_occupancy()}}
for the occupancy-comparison calculation

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}}
}
\concept{pipeline stages}
