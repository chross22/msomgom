% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model.R
\name{plot_season_panels}
\alias{plot_season_panels}
\title{Map every season on one shared scale}
\usage{
plot_season_panels(
  x,
  arrays = NULL,
  what = c("coverage", "sightings"),
  species = NULL,
  kind = c("surface", "probability"),
  seasons = NULL,
  label = NULL,
  title = NULL
)
}
\arguments{
\item{x}{either an \code{arrays} list from \code{\link[=build_detection_arrays]{build_detection_arrays()}} (for
\code{"coverage"} or \code{"sightings"}), or a \verb{[num_cells x num_ssn]} matrix such
as one element of \code{\link[=average_covariates]{average_covariates()}}'s return value}

\item{arrays}{the list from \code{\link[=build_detection_arrays]{build_detection_arrays()}}, for the grid; taken
from \code{x} when \code{x} is itself an arrays list}

\item{what}{for an arrays list: \code{"coverage"} or \code{"sightings"}}

\item{species}{species code, when \code{what} is \code{"sightings"}}

\item{kind}{\code{"surface"} for a positive quantity, \code{"probability"} for a 0-1
one}

\item{seasons}{which seasons to draw; \code{NULL} (the default) draws them all}

\item{label}{what to call the quantity in the shared legend. Supplying one
is what makes the legend shared: without it each panel names its own
scale, and one scale drawn per panel reads as several scales}

\item{title}{figure title}
}
\value{
invisibly, the \code{sf} grid carrying one column per drawn season, with
the \code{ggplot} attached as the \code{"plot"} attribute
}
\description{
Small multiples of a per-season quantity - occupancy, effort, sightings -
drawn on a single scale, so the panels can be compared. Panels drawn
separately cannot be: each gets its own scale, and a cell that looks dark
in June and light in July may be identical in both.
}
\examples{
\dontrun{
plot_season_panels(arrays, what = "coverage")
plot_season_panels(sst_avg$sst, arrays, kind = "surface")
}
}
\seealso{
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}} and \code{\link[=plot_sightings]{plot_sightings()}} for one season at
a time; \code{fancymaps::map_panels()}, which draws this

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_occupancy_trend]{plot_occupancy_trend()}},
\code{\link[=plot_prior_posterior]{plot_prior_posterior()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
