% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/average_covariates.R
\name{average_covariates}
\alias{average_covariates}
\title{Spatially and temporally average covariates onto the occupancy grid}
\usage{
average_covariates(env_dat, area_grid_sf, windows, vars = NULL)
}
\arguments{
\item{env_dat}{\code{sf} POINT object with \code{YEAR}/\code{MONTH}/\code{DAY} columns and one
column per covariate variable (as returned by \code{load_covariate_netcdf()}
or \code{datamatch::accessEnvDat()})}

\item{area_grid_sf}{\code{sf} polygon grid with a \code{grid_id} column (e.g.
\code{arrays$area_grid_sf} from a prior \code{build_detection_arrays()} call, so
the grid matches exactly)}

\item{windows}{data.frame with \code{start_date}/\code{end_date}/\code{label} columns
(e.g. from \code{season_windows_from_config()} or \code{regular_windows()})}

\item{vars}{covariate column names in \code{env_dat} to average; if \code{NULL},
uses every column except \code{YEAR}/\code{MONTH}/\code{DAY}/geometry}
}
\value{
named list of \verb{[num_cells x nrow(windows)]} matrices, one per
covariate variable, with columns ordered/labeled to match \code{windows$label}
(\code{windows$ssn_no} if you used \code{season_windows_from_config()}, so column
\code{t} lines up with year \code{t} / absolute season index \code{t} elsewhere in this
pipeline)
}
\description{
Averages every variable column in \code{env_dat} over each grid cell x window
combination.
}
\examples{
\dontrun{
config <- load_config("configs/bof_riwh.yaml")
prep <- prep_survey_data(config)
arrays <- build_detection_arrays(prep$tmpdat, prep$season_info, config)

env_dat <- load_covariate_netcdf("data/covariates/sst", var_names = "sst")
windows <- season_windows_from_config(config)
sst_avg <- average_covariates(env_dat, arrays$area_grid_sf, windows)
}
}
\seealso{
\code{\link[=season_windows_from_config]{season_windows_from_config()}} and \code{\link[=regular_windows]{regular_windows()}} for
building \code{windows}; \code{\link[=load_covariate_netcdf]{load_covariate_netcdf()}} for building \code{env_dat};
\code{\link[=build_detection_arrays]{build_detection_arrays()}} for \code{area_grid_sf}; \code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
which takes this function's output as \code{occ_covariates}

Other covariates:
\code{\link[=load_covariate_netcdf]{load_covariate_netcdf()}}
}
\concept{covariates}
