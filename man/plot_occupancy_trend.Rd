% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model.R
\name{plot_occupancy_trend}
\alias{plot_occupancy_trend}
\title{Plot occupancy across seasons, modeled against naive}
\usage{
plot_occupancy_trend(fit, arrays, config, level = 0.95, naive = TRUE)
}
\arguments{
\item{fit}{an \code{mcmc.list} that tracked \code{Z}, or a path to a saved fit}

\item{arrays}{the list from \code{\link[=build_detection_arrays]{build_detection_arrays()}}}

\item{config}{a config list, as returned by \code{\link[=load_config]{load_config()}}}

\item{level}{credible-interval level for the band (default 0.95)}

\item{naive}{whether to draw the naive series alongside (default \code{TRUE})}
}
\value{
invisibly, the data.frame behind the figure - one row per season
with \code{season}, \code{naive_psi}, \code{modeled_psi}, \code{lower}, \code{upper} - with the
\code{ggplot} attached as the \code{"plot"} attribute
}
\description{
The single figure that answers "is this species occupying more or less of
the study area over time" - the question a multi-season occupancy model is
usually fit to answer, and one that \code{compare_naive_vs_modeled_occupancy()}
currently only reports as a table.
}
\details{
Both series are drawn: the modeled trajectory with its credible band, and
the naive proportion of surveyed cells with a detection. The gap between
them is the detection correction, which is the reason to fit the model at
all - if they sit on top of each other, detection is near certain and the
model is doing little; if the modeled line runs far above, most occupied
cells were never seen, and the naive series would have understated
occupancy badly.

Requires a \code{"Z"} fit, since it is the latent states that give occupancy
per season. A \code{"colext"} fit tracks coefficients instead - see
\code{\link[=occupancy_summary]{occupancy_summary()}} for what that offers, and \code{\link[=plot_process_map]{plot_process_map()}} for
the surfaces it implies.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config) # config$jags$params must be "Z"
plot_occupancy_trend(fit, arrays, config)
}
}
\seealso{
\code{\link[=compare_naive_vs_modeled_occupancy]{compare_naive_vs_modeled_occupancy()}} for the same numbers as a
table; \code{\link[=occupancy_summary]{occupancy_summary()}} for the derived quantities behind them

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_prior_posterior]{plot_prior_posterior()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_season_panels]{plot_season_panels()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
