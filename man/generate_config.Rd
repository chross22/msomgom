% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_config.R
\name{generate_config}
\alias{generate_config}
\title{Write a pipeline config YAML file}
\usage{
generate_config(
  name,
  configs_dir = "configs",
  overwrite = FALSE,
  project_dir = ".",
  data_file = "data/survey_data.csv",
  google_drive_filename = NULL,
  onedrive_filename = NULL,
  onedrive_type = "personal",
  output_dir = "output",
  platform_code = 99,
  fileid_prefixes = c("P", "p"),
  on_effort_legtypes = c(5, 6),
  beg_year = 1988,
  end_year = 2020,
  beg_month = 8,
  end_month = 9,
  seasons = list(list(begin = c(8, 1), end = c(8, 31)), list(begin = c(9, 1), end = c(9,
    30))),
  cell_size = 0.05,
  polygon = list(c(-66.45, 44.82), c(-66.28, 44.78), c(-66.28, 44.67), c(-66.37, 44.55),
    c(-66.5, 44.48), c(-66.62, 44.48), c(-66.62, 44.7), c(-66.45, 44.82)),
  species_codes = c("RIWH"),
  active_species = species_codes[1],
  covariates_psi = character(0),
  covariates_phi = character(0),
  covariates_gamma = character(0),
  make_figs = FALSE,
  n_chains = 3,
  n_adapt = 5000,
  n_burn = 5000,
  n_iter = 20000,
  thin = 1,
  jags_params = "colext",
  save_results = TRUE,
  run_evaluation = TRUE,
  save_evaluation_plots = TRUE,
  cleanup_after_run = FALSE
)
}
\arguments{
\item{name}{config name; written to \verb{<configs_dir>/<name>.yaml}}

\item{configs_dir}{directory to write the config into}

\item{overwrite}{logical; if \code{FALSE} (the default), warns and leaves an
existing config with the same \code{name} unchanged rather than overwriting it}

\item{project_dir}{base directory that relative paths in the config resolve against}

\item{data_file}{path to the survey CSV, relative to \code{project_dir} unless absolute}

\item{google_drive_filename}{optional; if set and \code{data_file} is missing,
\code{prep_survey_data()} downloads this filename from Google Drive}

\item{onedrive_filename}{optional; if set and \code{data_file} is missing (and
\code{google_drive_filename} isn't also set), \code{prep_survey_data()} downloads
this path from OneDrive via the \code{Microsoft365R} package - the path as it
appears within OneDrive, not a local path}

\item{onedrive_type}{\code{"personal"} (the default) or \code{"business"} - which
\code{Microsoft365R} account type \code{onedrive_filename} is downloaded from
(\code{get_personal_onedrive()}/\code{get_business_onedrive()}); a shared/org
OneDrive is usually \code{"business"}}

\item{output_dir}{directory for model outputs, relative to \code{project_dir} unless absolute}

\item{platform_code}{NARWC \code{PLATFORM} code for the survey platform (e.g. 99 = R/V Nereid)}

\item{fileid_prefixes}{\code{FILEID} first-letter codes to keep (e.g. \code{c("P", "p")} for POP shipboard surveys)}

\item{on_effort_legtypes}{\code{LEGTYPE} codes that count as on-effort (combined
with \code{LEGSTAGE} begin/continue/end-watch, which - per NARWC 8.A.20 - is
recorded independently of \code{LEGTYPE} and shared across POP ship and aerial
surveys alike, so it isn't platform-specific). Defaults to \code{c(5, 6)},
NARWC 8.A.21's codes for a POP shipboard survey ("ship underway" /
"ship not underway"). A POP aerial survey uses \code{7} ("POP aerial") and
\code{9} ("POP aerial, but with restricted data-recording" - used by some
Southeast surveys after 2003; the handbook notes this variant may not be
usable for every species, e.g. usable for right whales but not
dolphins/turtles, so consider whether to include \code{9} for your species).
This pipeline assumes POP-format survey data; NARWC's separate
line-transect \code{LEGTYPE} scheme (codes \code{0}-\code{4}) isn't supported, since
\code{LEGSTAGE} means something different there (recorded only during
census legs, \code{LEGTYPE == 2}, rather than independently).}

\item{beg_year}{first year to include (inclusive)}

\item{end_year}{last year to include (inclusive)}

\item{beg_month}{first month to include}

\item{end_month}{last month to include}

\item{seasons}{list of \code{list(begin = c(month, day), end = c(month, day))}, one per season}

\item{cell_size}{grid cell size, in decimal degrees}

\item{polygon}{list of \code{c(lon, lat)} vertices, or an n x 2 matrix/data.frame
of lon/lat, describing the study-area boundary}

\item{species_codes}{species (\code{SPECCODE}) to build detection-history arrays for}

\item{active_species}{which of \code{species_codes} \code{fit_occupancy_model()} actually fits this run}

\item{covariates_psi}{names of covariates (matching names in the
\code{occ_covariates} list passed to \code{run_occupancy_model()}/\code{fit_occupancy_model()})
to put on initial occupancy. Empty (the default) means occupancy stays
intercept-only, exactly as before this option existed.}

\item{covariates_phi}{as \code{covariates_psi}, but for persistence}

\item{covariates_gamma}{as \code{covariates_psi}, but for colonization}

\item{make_figs}{logical; if \code{TRUE}, \code{build_detection_arrays()} saves survey
maps (requires \code{mapview}/\code{tmap}/\code{webshot})}

\item{n_chains}{number of MCMC chains}

\item{n_adapt}{number of JAGS adaptation iterations}

\item{n_burn}{number of burn-in iterations}

\item{n_iter}{number of sampling iterations (post burn-in)}

\item{thin}{thinning interval}

\item{jags_params}{\code{"colext"} (track community-level hyperparameters) or
\code{"Z"} (track occupancy states directly)}

\item{save_results}{logical; whether \code{fit_occupancy_model()} saves the fitted \code{.RData} result}

\item{run_evaluation}{logical; whether \code{run_occupancy_model()} calls \code{evaluate_occupancy_model()} after fitting}

\item{save_evaluation_plots}{logical; whether \code{evaluate_occupancy_model()} saves trace/density plots}

\item{cleanup_after_run}{logical; whether \code{run_occupancy_model()} calls
\code{cleanup_outputs()} after fitting (deletes the downloaded data file and \verb{figs/})}
}
\value{
the path the config was written to, invisibly
}
\description{
Programmatically writes a config YAML file into \verb{configs/}, so multiple
runs (different study areas, species, date ranges, MCMC settings, ...) can
be kept as separate named files instead of hand-editing one \code{config.yaml}.
}
\details{
Defaults reproduce the pipeline's original Bay of Fundy / RIWH settings, so
\code{generate_config("some_name")} with no other arguments is a working config.
Requires the \code{yaml} package (\code{load_config.R} also depends on it).
}
\examples{
\dontrun{
# reproduces the pipeline's original Bay of Fundy / RIWH settings
generate_config("bof_riwh")

# a different study, with covariates on occupancy and persistence
generate_config(
  "my_run",
  data_file = "data/my_survey_data.csv",
  beg_year = 2015, end_year = 2020,
  species_codes = c("RIWH", "HUWH"), active_species = "HUWH",
  covariates_psi = c("sst"), covariates_phi = c("sst")
)

# a POP aerial survey instead of a vessel one: on_effort_legtypes = c(7, 9)
# is NARWC 8.A.21's real POP-aerial codes (7 = normal, 9 = restricted
# data-recording); platform_code/fileid_prefixes are illustrative - look up
# the actual PLATFORM code and FILEID prefix for your specific aircraft/survey
generate_config(
  "aerial_run",
  data_file = "data/aerial_survey_data.csv",
  platform_code = 47, fileid_prefixes = c("A", "a"),
  on_effort_legtypes = c(7, 9)
)
}
}
\seealso{
\code{\link[=load_config]{load_config()}} to read the config back in; the fields
documented here match \code{configs/bof_riwh.yaml}
}
\concept{config}
