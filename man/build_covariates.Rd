% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_covariates.R
\name{build_covariates}
\alias{build_covariates}
\title{Build every configured covariate, from the config alone}
\usage{
build_covariates(config, area_grid_sf, windows = NULL)
}
\arguments{
\item{config}{a config list, as returned by \code{\link[=load_config]{load_config()}}}

\item{area_grid_sf}{the \code{sf} polygon grid to average onto, with a \code{grid_id}
column - \code{arrays$area_grid_sf} from \code{\link[=build_detection_arrays]{build_detection_arrays()}}, so the
covariate grid matches the model's exactly}

\item{windows}{data.frame of covariate windows; defaults to
\code{\link[=season_windows_from_config]{season_windows_from_config()}}, so column \code{t} lines up with season \code{t}}
}
\value{
a named list of \verb{[num_cells x num_ssn]} matrices, one per built
covariate column, ready to pass to \code{\link[=fit_occupancy_model]{fit_occupancy_model()}} as
\code{occ_covariates}. \code{NULL} when no process has any covariates configured
}
\description{
The whole covariate stage in one call: run each recipe under
\code{config$covariates$sources} (fetch with a \code{datamatch} access function,
derive with any number of \code{derivoce} functions), average every resulting
column onto the occupancy grid and season windows, and return the named
list of \verb{[num_cells x num_ssn]} matrices that \code{\link[=fit_occupancy_model]{fit_occupancy_model()}} takes
as \code{occ_covariates}.
}
\details{
Any covariate either package can produce is usable, because a recipe names
the function rather than choosing from a fixed menu here:

\if{html}{\out{<div class="sourceCode yaml">}}\preformatted{covariates:
  psi: [SST, shore_dist]        # names of columns, not of recipes
  phi: [SST_grad]
  gamma: []
  sources:
    ocean:
      fn: accessCopernicus      # any datamatch access function
      args:
        vars: [SST, UO, VO]     # years/months/bounding_box are implied
      derive:                   # any number of derivoce functions, in order
        - fn: horizontal_gradient
          args: \{vars: SST\}     # adds SST_grad
        - fn: eke               # adds EKE from UO/VO
        - fn: distance_to_shore # adds shore_dist
}\if{html}{\out{</div>}}

A recipe is a \emph{pipeline}, not a single covariate: one recipe can produce
many columns, and \code{psi}/\code{phi}/\code{gamma} name the columns they want from
everything produced. Names are checked against what was actually built, so
a covariate that was configured on a process but never produced is an error
naming both sides rather than a model fit without it.
}
\examples{
\dontrun{
config <- load_config("configs/bof_riwh.yaml")
prep <- prep_survey_data(config)
arrays <- build_detection_arrays(prep$tmpdat, prep$season_info, config)

occ_covariates <- build_covariates(config, arrays$area_grid_sf)
fit <- fit_occupancy_model(arrays, config, occ_covariates = occ_covariates)
}
}
\seealso{
\code{\link[=average_covariates]{average_covariates()}}, which does the averaging;
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, which consumes the result;
\code{\link[=plot_covariate_map]{plot_covariate_map()}} for checking one before fitting;
\code{datamatch::accessCopernicus()} and \code{derivoce::horizontal_gradient()} for
the two kinds of function a recipe names

Other covariates:
\code{\link[=average_covariates]{average_covariates()}},
\code{\link[=effect_estimates.dynocc_fit]{effect_estimates.dynocc_fit()}},
\code{\link[=load_covariate_netcdf]{load_covariate_netcdf()}},
\code{\link[=predict.dynocc_fit]{predict.dynocc_fit()}}
}
\concept{covariates}
