% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose_pipeline}
\alias{diagnose_pipeline}
\title{Diagnose common reasons a run might fail, hang, or produce meaningless output}
\usage{
diagnose_pipeline(config_path, occ_covariates = NULL)
}
\arguments{
\item{config_path}{path to a config YAML, or an already-loaded config
list (as returned by \code{load_config()})}

\item{occ_covariates}{optional named list, as passed to
\code{fit_occupancy_model()}/\code{run_occupancy_model()} - checked for missing
names and shape mismatches against \code{config$covariates$psi/phi/gamma}}
}
\value{
invisibly, \code{list(config, prep, arrays)} - whichever of these were
reached before a fatal problem stopped the checks - so you can pick up
investigating from there, e.g. \code{plot_survey_coverage(result$arrays)}
}
\description{
Runs \code{prep_survey_data()} and \code{build_detection_arrays()} - never JAGS
itself - and reports on the most common ways a run goes wrong before it
ever reaches the model: a filter that leaves nothing, a study-area
polygon that misses the survey tracks, a species with zero detections, a
season nothing got assigned to, or a covariate matrix with the wrong
shape. Meant to run \emph{before} \code{run_occupancy_model()}/\code{fit_occupancy_model()},
so a misconfiguration is caught in seconds rather than after a slow JAGS
run that either errors cryptically or "succeeds" with meaningless output.
}
\details{
Every check here reports a problem rather than fixing it - this function
never modifies the config, the data, or the returned objects.
}
\examples{
\dontrun{
diagnose_pipeline("configs/my_run.yaml")
diagnose_pipeline("configs/my_run.yaml", occ_covariates = list(sst = sst_avg$sst))
}
}
\seealso{
\code{\link[=prep_survey_data]{prep_survey_data()}}'s \code{verbose} argument, which this uses;
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}} and \code{\link[=plot_sightings]{plot_sightings()}} for visualizing what
this function reports as plain numbers; \code{\link[=run_occupancy_model]{run_occupancy_model()}}, which
this is meant to run ahead of

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}},
\code{\link[=standardize_survey_columns]{standardize_survey_columns()}}
}
\concept{pipeline stages}
