% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{run_occupancy_model}
\alias{run_occupancy_model}
\title{Run the full occupancy-model pipeline}
\usage{
run_occupancy_model(config_path, occ_covariates = NULL)
}
\arguments{
\item{config_path}{path to a config YAML file (see \code{load_config()})}

\item{occ_covariates}{optional named list of covariate matrices for
\code{fit_occupancy_model()}; see its documentation}
}
\value{
\verb{list(fit = <mcmc.list>, evaluation = <evaluate_occupancy_model() result, or NULL if skipped>)}
}
\description{
Runs every pipeline stage in order: \code{prep_survey_data()} (load + clean the
survey CSV) -> \code{build_detection_arrays()} (spatial grid + detection-history
arrays) -> \code{fit_occupancy_model()} (fit the model) ->
\code{evaluate_occupancy_model()} (diagnostics, unless disabled in the config)
-> \code{cleanup_outputs()} (only if \code{cleanup.run_after_model: true}).
}
\examples{
\dontrun{
library(msomgom)
result <- run_occupancy_model("configs/bof_riwh.yaml")
result$fit          # the fitted mcmc.list
result$evaluation    # convergence diagnostics / parameter summary, see evaluate_occupancy_model()

# To put environmental covariates on occupancy/persistence/colonization,
# list their names under a config's covariates.psi/phi/gamma (see
# configs/bof_riwh.yaml), build them yourself (e.g. with
# average_covariates()) into a named list of [num_cells x num_ssn]
# matrices, and pass that in:
result <- run_occupancy_model("configs/my_run.yaml", occ_covariates = my_covariates)
}

}
\seealso{
\code{\link[=load_config]{load_config()}}, \code{\link[=prep_survey_data]{prep_survey_data()}}, \code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, \code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}}, and
\code{\link[=cleanup_outputs]{cleanup_outputs()}} - the individual stages this function orchestrates

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}}
}
\concept{pipeline stages}
