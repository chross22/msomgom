% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_process.R
\name{predict.dynocc_fit}
\alias{predict.dynocc_fit}
\title{Predict a process's probability surface across the study grid}
\usage{
\method{predict}{dynocc_fit}(
  object,
  arrays,
  process = c("psi", "phi", "gamma"),
  occ_covariates = NULL,
  season = NULL,
  level = 0.95,
  ...
)
}
\arguments{
\item{object}{a \code{dynocc_fit} from \code{fit_occupancy_model()} (must be a
\code{"colext"} fit - a \code{"Z"} fit tracks states, not coefficients)}

\item{arrays}{the list returned by \code{build_detection_arrays()} (must be the
same one \code{object} was fit against, so cells line up)}

\item{process}{which process to predict: \code{"psi"} (initial occupancy),
\code{"phi"} (persistence), or \code{"gamma"} (colonization)}

\item{occ_covariates}{the same named list of \verb{[num_cells x num_ssn]}
matrices that was passed to \code{fit_occupancy_model()} (e.g. from
\code{\link[=average_covariates]{average_covariates()}}). Required when \code{process} has covariates
configured; ignored otherwise}

\item{season}{which season's covariate values to predict at
(\code{1:arrays$num_ssn}); defaults to the last season}

\item{level}{credible-interval level for \code{lower}/\code{upper} (default 0.95)}

\item{...}{ignored; present to match \code{\link[stats:predict]{stats::predict()}}'s signature}
}
\value{
a data.frame with one row per grid cell: \code{grid_id}, \code{mean}, \code{sd},
\code{lower}, \code{upper} (posterior mean, SD, and credible interval of the
process probability), with attributes \code{process}, \code{season}, and \code{level}
}
\description{
Turns a coefficient-level (\code{jags_params = "colext"}, the default) fit into
a per-cell probability surface for one of the three occupancy processes:
initial occupancy (\code{psi}), persistence (\code{phi}), or colonization (\code{gamma}).
Each posterior draw's coefficients are pushed through that draw's inverse
logit at every cell, so the summaries are true posterior summaries of the
surface, not transformations of coefficient summaries.
}
\details{
This is the spatial view a \code{colext} fit implies but never stores: \code{Z} isn't
tracked, so there is no realized occupancy map (\code{\link[=plot_occupancy_map]{plot_occupancy_map()}}
needs \code{jags_params = "Z"} for that) - but wherever a process has covariates,
its fitted probability varies cell by cell, and this reconstructs exactly
the linear predictor the JAGS model used, including the fit's own
standardization of each covariate (stored on the fit at fitting time).

Two honest limitations, both worth knowing when reading a map:
the year-level intercepts (\code{b.0[t]}, \code{e.0[t]}, \code{g.0[t]}) are not tracked,
only their hyper-means (\code{mu.b.0}, ...), so this is the population-level
surface - \code{season} moves the map only through the covariate values, not
through a year effect. And a process with no covariates configured is
spatially flat by construction; the surface is still computed (it's the
posterior of the intercept, which is not nothing), with a message saying
so.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config, occ_covariates = list(sst = sst_avg$sst))
predict(fit, arrays, "psi", occ_covariates = list(sst = sst_avg$sst))
}
}
\seealso{
\code{\link[=plot_process_map]{plot_process_map()}}, which draws this;
\code{\link[=effect_estimates.dynocc_fit]{effect_estimates.dynocc_fit()}} for the same coefficients as a curve
along one covariate; \code{\link[=plot_occupancy_map]{plot_occupancy_map()}} for realized occupancy from
a \code{"Z"} fit

Other covariates:
\code{\link[=average_covariates]{average_covariates()}},
\code{\link[=build_covariates]{build_covariates()}},
\code{\link[=effect_estimates.dynocc_fit]{effect_estimates.dynocc_fit()}},
\code{\link[=load_covariate_netcdf]{load_covariate_netcdf()}}
}
\concept{covariates}
