% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeSeasons.R
\name{makeSeasons}
\alias{makeSeasons}
\title{Build a year x season lookup table}
\usage{
makeSeasons(begYEAR, endYEAR, ssn_beg, ssn_end)
}
\arguments{
\item{begYEAR}{first year (inclusive)}

\item{endYEAR}{last year (inclusive)}

\item{ssn_beg}{matrix with one row per season, columns \code{c(month, day)} for
the season's start date, e.g. \code{rbind(c(8, 1), c(9, 1))}}

\item{ssn_end}{matrix with one row per season, columns \code{c(month, day)} for
the season's end date, e.g. \code{rbind(c(8, 31), c(9, 30))}}
}
\value{
data.frame with one row per year x season combination and columns
\code{Y}, \code{M_BEG}, \code{D_BEG}, \code{M_END}, \code{D_END}, \code{JDAY_BEG}, \code{JDAY_END},
\code{SSN_NO} (absolute index), \code{SSN_GRPD_NO} (index within year)
}
\description{
For every year in \code{begYEAR:endYEAR} and every season defined by
\code{ssn_beg}/\code{ssn_end}, computes the season's start/end day-of-year and an
absolute season index (\code{SSN_NO}, unique across all year x season
combinations) plus a within-year season index (\code{SSN_GRPD_NO}). Used by
\code{data_prep.R} to assign each survey record a season, and downstream by
\code{jagsPrep.R}/\code{jags.R}, where \code{SSN_NO}'s range (\code{1:max(SSN_NO)}) is treated
as the model's \code{num_ssn}/\code{n.year} dimension.
}
\examples{
makeSeasons(2020, 2021, rbind(c(8, 1), c(9, 1)), rbind(c(8, 31), c(9, 30)))
}
\seealso{
\code{\link[=prep_survey_data]{prep_survey_data()}} and \code{\link[=season_windows_from_config]{season_windows_from_config()}}, both of
which call this with \code{config$ssn_beg}/\code{config$ssn_end} (see \code{\link[=load_config]{load_config()}})

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}}
}
\concept{pipeline stages}
