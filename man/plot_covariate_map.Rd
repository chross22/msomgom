% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_covariate_map}
\alias{plot_covariate_map}
\title{Map a covariate across the study grid}
\usage{
plot_covariate_map(
  cov,
  arrays,
  window = NULL,
  var_name = NULL,
  main = NULL,
  ...
)
}
\arguments{
\item{cov}{a \verb{[num_cells x num_windows]} covariate matrix, i.e. one
element of \code{average_covariates()}'s return list (e.g. \code{sst_avg$sst})}

\item{arrays}{the list returned by \code{build_detection_arrays()}; \code{cov} must
come from \code{average_covariates()} called with this same \code{arrays$area_grid_sf},
so rows line up}

\item{window}{which column of \code{cov} to show, by position or by
\code{colnames(cov)} label (e.g. a \code{windows$label} value); defaults to the
last column}

\item{var_name}{name to use in the auto-generated title (e.g. \code{"sst"});
ignored if \code{main} is given}

\item{main}{plot title; auto-generated from \code{var_name}/\code{window} if \code{NULL}}

\item{...}{passed on to \code{plot()}}
}
\value{
invisibly, \code{arrays$area_grid_sf} with a \code{covariate} column added
(the chosen window's values)
}
\description{
Plots one window's worth of a covariate matrix - e.g. \code{sst_avg$sst} from
\code{average_covariates()} - across the grid, so it can be checked visually
before it goes into \code{fit_occupancy_model()}. A covariate that's \code{NA}
everywhere, constant, or has an unexpected spatial pattern is much faster
to spot here than after a fit quietly does nothing with it.
}
\examples{
\dontrun{
sst_avg <- average_covariates(env_dat, arrays$area_grid_sf, windows)
plot_covariate_map(sst_avg$sst, arrays, var_name = "sst")
plot_covariate_map(sst_avg$sst, arrays, window = 3, var_name = "sst")
}
}
\seealso{
\code{\link[=average_covariates]{average_covariates()}}, which produces \code{cov}; \code{\link[=plot_survey_coverage]{plot_survey_coverage()}},
\code{\link[=plot_sightings]{plot_sightings()}}, \code{\link[=plot_occupancy_map]{plot_occupancy_map()}}

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
