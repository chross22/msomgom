% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{prep_survey_data}
\alias{prep_survey_data}
\title{Load and clean the vessel survey CSV}
\usage{
prep_survey_data(config, verbose = FALSE)
}
\arguments{
\item{config}{a config list, as returned by \code{load_config()}}

\item{verbose}{logical; if \code{TRUE}, reports how many records survive each
filtering step (platform, \code{FILEID} prefix, date range), and which of those
filters were skipped as unset - useful for diagnosing a run that ends up
with suspiciously little data. See also \code{\link[=diagnose_pipeline]{diagnose_pipeline()}}, which calls
this with \code{verbose = TRUE} alongside other checks.}
}
\value{
list with:
\item{dat}{the full cleaned dataset}
\item{tmpdat}{a reduced dataset with just the columns needed for gridding}
\item{season_info}{list(season = <season lookup table from \code{makeSeasons()}>, num_ssn = \if{html}{\out{<int>}})}
}
\description{
Ready for spatial gridding in \code{jagsPrep.R::build_detection_arrays()}.
Combines what used to be two separate, drifted implementations
(\code{legacy/master.R} and the pre-refactor \code{data_prep.R}): master.R's GMT ->
US/Eastern datetime conversion (more correct near UTC day boundaries than
filtering on the raw YEAR/MONTH/DAY columns) plus data_prep.R's
BEHAV*-column drop.
}
\details{
\code{survey.platform_code} and \code{survey.fileid_prefixes} are both required, and
an unset one errors rather than being treated as "keep everything":
\code{build_detection_arrays()} assumes a single survey type (one \code{FILEID} = one
single-day survey = one replicate column), and the detection model has no
platform covariate, so gridding several platforms together produces a fit
that doesn't mean what it looks like. \code{platform_code} accepts more than one
code for a genuinely combined analysis, but that's a deliberate choice.
}
\examples{
\dontrun{
config <- load_config("configs/bof_riwh.yaml")
prep <- prep_survey_data(config)
prep$season_info$num_ssn

# see how many records each filter step drops
prep <- prep_survey_data(config, verbose = TRUE)
}
}
\seealso{
\code{\link[=load_config]{load_config()}}, which produces \code{config}; \code{\link[=build_detection_arrays]{build_detection_arrays()}},
the next pipeline stage, which takes this function's \code{tmpdat}/\code{season_info};
\code{\link[=diagnose_pipeline]{diagnose_pipeline()}}, for a friendlier pre-flight check than reading this
function's warnings after the fact

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=diagnose_pipeline]{diagnose_pipeline()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}},
\code{\link[=standardize_survey_columns]{standardize_survey_columns()}}
}
\concept{pipeline stages}
