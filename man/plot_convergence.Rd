% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_convergence}
\alias{plot_convergence}
\title{Plot MCMC convergence at a glance}
\usage{
plot_convergence(
  fit,
  rhat_threshold = 1.1,
  ess_threshold = 100,
  main = "MCMC convergence",
  ...
)
}
\arguments{
\item{fit}{an \code{mcmc.list} (what \code{fit_occupancy_model()} returns), a path to
a saved fit, or the list \code{evaluate_occupancy_model()} returns invisibly
(its \verb{$parameters} data.frame is used directly, skipping recomputation)}

\item{rhat_threshold}{Rhat above this is flagged (default 1.1)}

\item{ess_threshold}{effective sample size below this is flagged
(default 100)}

\item{main}{plot title}

\item{...}{passed on to \code{plot()}}
}
\value{
invisibly, a data.frame with columns \code{parameter}, \code{eff_size},
\code{rhat}, and \code{flagged}
}
\description{
Every tracked parameter as one point: Gelman-Rubin Rhat against effective
sample size, with the conventional thresholds drawn (Rhat < 1.1,
ESS >= 100 - the same ones \code{evaluate_occupancy_model()} warns about).
Parameters outside either threshold are labeled by name.
}
\details{
The two axes fail differently: high Rhat means the chains disagree about
where the posterior is (run longer, or reparameterize), while low ESS with
good Rhat means they agree but mix slowly (thin less, run longer, or accept
noisier estimates). A point bad on both axes usually indicates a parameter
the data barely inform - check the detection history
(\code{\link[=plot_detection_history]{plot_detection_history()}}) before buying more iterations.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config)
plot_convergence(fit)
}
}
\seealso{
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}}, which computes the same diagnostics
as numbers; \code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, which produces \code{fit}

Other diagnostic plots:
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
