% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_survey_coverage}
\alias{plot_survey_coverage}
\title{Map survey coverage across the study grid}
\usage{
plot_survey_coverage(arrays, season = NULL, main = NULL, ...)
}
\arguments{
\item{arrays}{the list returned by \code{build_detection_arrays()}}

\item{season}{which season (\code{1:arrays$num_ssn}) to show; \code{NULL} (the
default) sums coverage across every season}

\item{main}{plot title; auto-generated from \code{season} if \code{NULL}}

\item{...}{passed on to \code{plot()}}
}
\value{
invisibly, \code{arrays$area_grid_sf} with an \code{n_surveys} column added
}
\description{
Plots the number of surveys that covered each grid cell, either summed
across every season or for one season alone. Built directly from \code{reps}
(\code{build_detection_arrays()}'s repeat-visit matrix), so it shows exactly
what the model saw as survey effort, not a rederivation from raw data.
}
\details{
A cell that's well-covered everywhere but the model still won't converge
points at the detection covariates (bft/jday/eff) or too few MCMC
iterations; a cell that's unexpectedly blank points at the study-area
polygon or grid construction instead.
}
\examples{
\dontrun{
arrays <- build_detection_arrays(prep$tmpdat, prep$season_info, config)
plot_survey_coverage(arrays)
plot_survey_coverage(arrays, season = 3)
}
}
\seealso{
\code{\link[=plot_sightings]{plot_sightings()}}, \code{\link[=plot_occupancy_map]{plot_occupancy_map()}}, \code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=build_detection_arrays]{build_detection_arrays()}}, which produces \code{arrays}

Other diagnostic plots:
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_sightings]{plot_sightings()}}
}
\concept{diagnostic plots}
