% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate_model.R
\name{compare_naive_vs_modeled_occupancy}
\alias{compare_naive_vs_modeled_occupancy}
\title{Compare naive vs. modeled occupancy per year}
\usage{
compare_naive_vs_modeled_occupancy(fit, arrays, config)
}
\arguments{
\item{fit}{an \code{mcmc.list} that tracked \code{Z}}

\item{arrays}{the list returned by \code{build_detection_arrays()}}

\item{config}{a config list, as returned by \code{load_config()}}
}
\value{
data.frame with columns \code{year}, \code{n_sites}, \code{naive_psi}, \code{modeled_psi}
}
\description{
Naive occupancy (proportion of sites with >=1 detection, ignoring
never-surveyed sites) vs. the model's posterior mean occupancy state (\code{Z}),
one row per year - mirrors the \code{psi.naive} calculation in \code{jags.R} so the
two are directly comparable. Requires \code{fit} to have tracked \code{Z} (i.e. the
model was run with \code{jags.params = "Z"}).
}
\seealso{
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}}, which calls this when \code{fit} tracked \code{Z}
}
\keyword{internal}
