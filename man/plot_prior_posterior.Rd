% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_model.R
\name{plot_prior_posterior}
\alias{plot_prior_posterior}
\title{Plot each parameter's posterior against the prior it started from}
\usage{
plot_prior_posterior(fit, parameters = NULL, prior_sd = NULL)
}
\arguments{
\item{fit}{a \code{dynocc_fit} from \code{\link[=fit_occupancy_model]{fit_occupancy_model()}} with
\code{jags_params = "colext"}}

\item{parameters}{which parameters to draw; \code{NULL} (the default) takes
every \verb{mu.*} the fit tracked}

\item{prior_sd}{standard deviation of the prior, on the logit scale -
defaults to the \code{dnorm(0, 0.1)} every \verb{mu.*} in this model is given}
}
\value{
invisibly, a data.frame of the drawn draws, with the \code{ggplot}
attached as the \code{"plot"} attribute
}
\description{
The check that catches the failure no convergence diagnostic can: a
parameter the data never informed, whose posterior is still its prior.
That arrives looking flawless - \code{Rhat} 1.00 and every draw effective,
because sampling a prior is easy - and it is exactly what hid this
package's inert colonization/persistence dynamics through every fit it had
produced.
}
\details{
One panel per parameter: the prior as a dashed line, the posterior filled
beneath it. A posterior sitting under its prior curve learned nothing, and
should not be interpreted whatever its credible interval says.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config)
plot_prior_posterior(fit)
}
}
\seealso{
\code{\link[=occupancy_summary]{occupancy_summary()}}, which reports the same check as a
contraction number per parameter; \code{\link[=plot_convergence]{plot_convergence()}}, which cannot see
this failure at all

Other diagnostic plots:
\code{\link[=plot_convergence]{plot_convergence()}},
\code{\link[=plot_covariate_map]{plot_covariate_map()}},
\code{\link[=plot_detection_history]{plot_detection_history()}},
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_occupancy_trend]{plot_occupancy_trend()}},
\code{\link[=plot_process_map]{plot_process_map()}},
\code{\link[=plot_season_panels]{plot_season_panels()}},
\code{\link[=plot_sightings]{plot_sightings()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
