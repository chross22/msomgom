% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/average_covariates.R
\name{load_covariate_netcdf}
\alias{load_covariate_netcdf}
\title{Load a folder of covariate NetCDF files}
\usage{
load_covariate_netcdf(nc_dir, var_names = NULL, pattern = "\\\\.nc$")
}
\arguments{
\item{nc_dir}{directory containing the NetCDF files}

\item{var_names}{optional names for the raster layers/variables; if \code{NULL},
uses whatever names the NetCDF files themselves carry}

\item{pattern}{regex to select files within \code{nc_dir}}
}
\value{
\code{sf} POINT object with one row per (grid-point, day)
}
\description{
Reads a folder of covariate NetCDF files (one file per day, or one file
with a daily time dimension - both are handled) into the same
sf-point-per-day shape \code{datamatch::accessEnvDat()} returns: columns \code{x},
\code{y}, one column per variable, \code{YEAR}, \code{MONTH}, \code{DAY}, geometry.
}
\details{
Date is read from each raster layer's time dimension when the file has one
(the normal case for Copernicus NetCDFs); otherwise it's parsed from the
filename, which must contain a \code{YYYY-MM-DD} or \code{YYYYMMDD} date (see
\code{parse_date_from_filename()}).
}
\examples{
\dontrun{
env_dat <- load_covariate_netcdf("data/covariates/sst", var_names = "sst")
}
}
\seealso{
\code{\link[=average_covariates]{average_covariates()}}, which consumes the result;
\code{\link[=parse_date_from_filename]{parse_date_from_filename()}} for the filename-date fallback;
\code{datamatch::accessEnvDat()} for fetching Copernicus data directly instead
of reading local files

Other covariates:
\code{\link[=average_covariates]{average_covariates()}}
}
\concept{covariates}
