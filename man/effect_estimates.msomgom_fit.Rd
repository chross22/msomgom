% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/effect_estimates.R
\name{effect_estimates.msomgom_fit}
\alias{effect_estimates.msomgom_fit}
\title{A fitted covariate's effect, in the shape fancyfx expects}
\usage{
\method{effect_estimates}{msomgom_fit}(
  model,
  var,
  scale = c("auto", "link", "response"),
  interval = c("auto", "se", "ci", "cri"),
  level = 0.95,
  n = 100,
  ...
)
}
\arguments{
\item{model}{an object from \code{fit_occupancy_model()} (class \code{msomgom_fit})}

\item{var}{name of the covariate whose effect to extract, as a string -
matching a name in \code{occ_covariates}/\code{config$covariates$psi/phi/gamma}}

\item{scale}{\code{"auto"} (the default, resolves to \code{"link"}), \code{"link"} for the
covariate's own contribution to the linear predictor (centered at zero at
the covariate's mean, like a GAM partial effect), or \code{"response"} for the
full predicted occupancy probability as the covariate varies, with any
other covariates on the same process held at their mean}

\item{interval}{\code{"auto"} (the default, resolves to \code{"ci"}), \code{"ci"}/\code{"cri"}
for the posterior credible interval at \code{level}, or \code{"se"} for a \verb{+/- 1}
posterior-SD band. \code{"auto"} resolves to \code{"ci"} because every msomgom fit
is summarized from posterior draws, the same as fancyfx treats a \code{brms}/
\code{rstanarm} fit - there is no standard error to build a \code{"se"} ribbon from
independent of the posterior itself.}

\item{level}{credible-interval level, used when \code{interval} is \code{"ci"}/\code{"cri"}}

\item{n}{number of points at which to evaluate the effect}

\item{...}{ignored; present so this matches \code{\link[fancyfx:effect_estimates]{fancyfx::effect_estimates()}}'s
signature}
}
\value{
a data.frame with columns \code{.x}, \code{.estimate}, \code{.lower}, \code{.upper},
and a \code{"quantity"} attribute naming what was computed - the same shape
\code{\link[fancyfx:effect_estimates]{fancyfx::effect_estimates()}} documents for every model class
}
\description{
Implements \code{\link[fancyfx:effect_estimates]{fancyfx::effect_estimates()}} for a \code{fit_occupancy_model()}
result, so \code{\link[fancyfx:plotEffects]{fancyfx::plotEffects()}}/\code{\link[fancyfx:plotSmooths]{fancyfx::plotSmooths()}} work on a
msomgom fit the same way they work on an \code{mgcv::gam} or any model
\code{marginaleffects} supports - without fancyfx needing to know anything about
this package. \code{fancyfx} is a \code{Suggests}-only dependency of msomgom, not the
other way around: this method lives here because interpreting a bare
\code{coda::mcmc.list} requires msomgom's own parameter-naming convention
(\code{mu.b.cov}, \code{mu.e.cov}, ...), which a generic plotting package has no way
to know.
}
\details{
\code{var} must be a covariate \code{fit_occupancy_model()} was actually run with
(one of \code{config$covariates$psi/phi/gamma}, matched by name against
\code{occ_covariates}), and configured on exactly one process - a covariate
shared across processes isn't supported here, since there would be more
than one effect to return. The model must have been fit with
\code{jags_params = "colext"} (the default), since that's what tracks the
\verb{mu.<prefix>.0}/\verb{mu.<prefix>.cov} coefficients this reads; a \code{"Z"} fit only
tracks occupancy states.

The evaluated grid spans the covariate's own observed range (from the raw
data passed to \code{fit_occupancy_model()}'s \code{occ_covariates}, not an arbitrary
multiple of its standard deviation), on the covariate's raw scale, so the
curve lines up with a rug drawn from that same raw data.
}
\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config, occ_covariates = list(sst = sst_avg$sst))

# standalone
effect_estimates(fit, "sst")

# or, with fancyfx installed, the effect curve with a rug of the raw data:
fancyfx::plotEffects(fit, data.frame(sst = as.vector(sst_avg$sst)), "sst")
}
}
\seealso{
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}}, which produces \code{model} and attaches the
covariate metadata this reads; \code{\link[=average_covariates]{average_covariates()}}, which produces the
covariate data in the first place

Other covariates:
\code{\link[=average_covariates]{average_covariates()}},
\code{\link[=load_covariate_netcdf]{load_covariate_netcdf()}}
}
\concept{covariates}
