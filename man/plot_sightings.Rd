% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_sightings}
\alias{plot_sightings}
\title{Map sighting counts across the study grid}
\usage{
plot_sightings(arrays, species, season = NULL, main = NULL, ...)
}
\arguments{
\item{arrays}{the list returned by \code{build_detection_arrays()}}

\item{species}{species code; must be a name in \code{arrays$species_arrays}}

\item{season}{which season (\code{1:arrays$num_ssn}) to show; \code{NULL} (the
default) sums sightings across every season}

\item{main}{plot title; auto-generated from \code{species}/\code{season} if \code{NULL}}

\item{...}{passed on to \code{plot()}}
}
\value{
invisibly, \code{arrays$area_grid_sf} with an \code{n_sightings} column added
}
\description{
Plots total sighting counts per grid cell for one species, from the
detection-history array \code{build_detection_arrays()} built for it - so this
shows exactly what feeds the model, not a rederivation from the raw
survey CSV.
}
\examples{
\dontrun{
arrays <- build_detection_arrays(prep$tmpdat, prep$season_info, config)
plot_sightings(arrays, "RIWH")
plot_sightings(arrays, "RIWH", season = 2)
}
}
\seealso{
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}, \code{\link[=plot_occupancy_map]{plot_occupancy_map()}}, \code{\link[=build_detection_arrays]{build_detection_arrays()}},
which produces \code{arrays}

Other diagnostic plots:
\code{\link[=plot_occupancy_map]{plot_occupancy_map()}},
\code{\link[=plot_survey_coverage]{plot_survey_coverage()}}
}
\concept{diagnostic plots}
