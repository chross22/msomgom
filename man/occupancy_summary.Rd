% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/occupancy_summary.R
\name{occupancy_summary}
\alias{occupancy_summary}
\alias{print.dynocc_summary}
\title{Summarize a fit the way it is read, not the way it was fit}
\usage{
occupancy_summary(
  fit,
  arrays = NULL,
  visits = NULL,
  level = 0.95,
  threshold = 0.05
)

\method{print}{dynocc_summary}(x, digits = 3, ...)
}
\arguments{
\item{fit}{a \code{dynocc_fit} from \code{\link[=fit_occupancy_model]{fit_occupancy_model()}} with
\code{jags_params = "colext"} (the default) - a \code{"Z"} fit tracks states, not
the coefficients this reads}

\item{arrays}{optionally the list from \code{\link[=build_detection_arrays]{build_detection_arrays()}}, used
only to take a realistic number of visits per surveyed cell-season for
the cumulative-detection row}

\item{visits}{number of visits for cumulative detection; defaults to the
median over surveyed cell-seasons in \code{arrays}, or 3 if \code{arrays} is not
given}

\item{level}{credible-interval level (default 0.95)}

\item{threshold}{contraction below which a parameter is reported as not
learned (default 0.05)}

\item{x}{a \code{dynocc_summary}}

\item{digits}{significant digits for the reported numbers}

\item{...}{ignored}
}
\value{
an object of class \code{dynocc_summary}: a list of \code{parameters} and
\code{derived} data.frames, both with \code{mean}/\code{lower}/\code{upper} columns, plus
\code{visits} and \code{level}. Has a \code{print()} method
}
\description{
Converts a \code{"colext"} fit's logit-scale hyper-means to probabilities,
derives the quantities a dynamic occupancy model is usually asked for
(equilibrium occupancy, turnover, cumulative detection), and flags any
parameter whose posterior barely moved from its prior.
}
\details{
Every transformation is applied to the posterior draws and summarized
afterwards, never to the summaries - \code{plogis(mean(x))} is not
\code{mean(plogis(x))}, and the difference is largest exactly where the
posterior is widest.
}
\section{What is reported}{

The parameter table gives each hyper-mean on the probability scale
(\code{psi}, \code{phi}, \code{gamma}, \code{p}), and any covariate coefficient on its own
scale - log-odds per standard deviation of the covariate, since
\code{fit_occupancy_model()} standardizes covariates before fitting, and since
a slope is not a probability.

The derived table answers:
\itemize{
\item \strong{equilibrium occupancy}, \code{gamma / (gamma + (1 - phi))} - where
occupancy settles if these dynamics ran indefinitely. Read against the
estimated initial occupancy: far apart means the system is not at
steady state over the seasons modeled.
\item \strong{turnover}, \code{gamma * (1 - psi_eq) / psi_eq} - the share of
occupied cells that were empty the season before. High turnover with
high persistence is a different system than either alone, and average
occupancy cannot tell them apart.
\item \strong{cumulative detection}, \code{1 - (1 - p)^visits} - the chance of
seeing the species at least once in an occupied cell over a season's
visits. This is the number that says how badly naive occupancy
understates the truth.
}
}

\section{The prior check}{

\code{contraction} is \code{1 - sd(posterior) / sd(prior)} on the logit scale. Near
1, the data determined the parameter; near 0, the posterior is the prior
wearing a posterior's name - which arrives looking like flawless
convergence (\code{Rhat} 1.00, every draw effective) because sampling a prior
is easy. \code{learned} is \code{FALSE} below \code{threshold}, and those parameters
should not be interpreted, whatever their credible interval says.
}

\examples{
\dontrun{
fit <- fit_occupancy_model(arrays, config)
occupancy_summary(fit, arrays)
}
}
\seealso{
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}} for convergence and the
naive-vs-modeled comparison; \code{\link[=plot_convergence]{plot_convergence()}}, which should be read
before this; \code{\link[=plot_process_map]{plot_process_map()}} for the same estimates in space

Other pipeline stages:
\code{\link[=build_detection_arrays]{build_detection_arrays()}},
\code{\link[=build_whale_model_code]{build_whale_model_code()}},
\code{\link[=cleanup_outputs]{cleanup_outputs()}},
\code{\link[=diagnose_pipeline]{diagnose_pipeline()}},
\code{\link[=evaluate_occupancy_model]{evaluate_occupancy_model()}},
\code{\link[=fit_occupancy_model]{fit_occupancy_model()}},
\code{\link[=load_config]{load_config()}},
\code{\link[=makeSeasons]{makeSeasons()}},
\code{\link[=padstr0]{padstr0()}},
\code{\link[=prep_survey_data]{prep_survey_data()}},
\code{\link[=run_occupancy_model]{run_occupancy_model()}},
\code{\link[=standardize_survey_columns]{standardize_survey_columns()}}
}
\concept{pipeline stages}
